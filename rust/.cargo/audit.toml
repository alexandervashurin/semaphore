# cargo-audit configuration
[advisories]
ignore = [
    "RUSTSEC-2023-0071",  # rsa via sqlx-postgres for SCRAM auth only
    "RUSTSEC-2026-0097",  # rand 0.8.5 unsound — transitive via sqlx/oauth2/wasmtime; direct rand updated to 0.9
]
