# Руководство по миграции с Go на Rust

Это руководство поможет вам перейти с Go-версии Semaphore UI на Rust-версию.

## 📋 Сравнение версий

| Характеристика | Go-версия | Rust-версия |
|----------------|-----------|-------------|
| Потребление памяти | ~50-100 MB | ~10-30 MB |
| Время запуска | ~1-2 сек | ~0.1-0.5 сек |
| Размер бинарника | ~50 MB | ~5-10 MB |
| Безопасность памяти | Garbage Collector | Compile-time гарантии |
| Конкурентность | Goroutines | Async/await (Tokio) |

## 🔄 Пошаговая миграция

### Шаг 1: Резервное копирование

Перед миграцией обязательно создайте резервную копию базы данных:

```bash
# Для BoltDB
cp -r /var/lib/semaphore /var/lib/semaphore.backup

# Для SQLite
cp /var/lib/semaphore/semaphore.db /var/lib/semaphore/semaphore.db.backup

# Для MySQL
mysqldump -u semaphore -p semaphore > semaphore_backup.sql

# Для PostgreSQL
pg_dump -U semaphore semaphore > semaphore_backup.sql
```

### Шаг 2: Установка Rust-версии

```bash
# Скачайте бинарный файл
wget https://github.com/semaphoreui/semaphore/releases/download/vX.Y.Z/semaphore-rust-linux-amd64.tar.gz
tar xzf semaphore-rust-linux-amd64.tar.gz
sudo mv semaphore /usr/local/bin/

# Или через cargo
cargo install semaphore
```

### Шаг 3: Обновление конфигурации

Конфигурация Rust-версии совместима с Go-версией на уровне переменных окружения.

```bash
# Пример systemd service
sudo nano /etc/systemd/system/semaphore.service
```

```ini
[Unit]
Description=Semaphore UI (Rust)
After=network.target

[Service]
Type=simple
User=semaphore
Environment="SEMAPHORE_DB_DIALECT=bolt"
Environment="SEMAPHORE_DB_PATH=/var/lib/semaphore"
Environment="RUST_LOG=info"
ExecStart=/usr/local/bin/semaphore server
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

### Шаг 4: Миграция базы данных

Rust-версия автоматически выполнит миграцию схемы базы данных при первом запуске.

```bash
# Запуск миграции
sudo -u semaphore semaphore migrate --upgrade

# Запуск сервера
sudo systemctl start semaphore
```

### Шаг 5: Проверка работы

```bash
# Проверка статуса
sudo systemctl status semaphore

# Проверка логов
sudo journalctl -u semaphore -f

# Проверка API
curl http://localhost:3000/api/health
```

## 🐛 Известные отличия

### CLI команды

Некоторые команды CLI могут иметь отличия в флагах:

| Go-версия | Rust-версия | Примечание |
|-----------|-------------|------------|
| `semaphore server -p 3000` | `semaphore server --port 3000` | Длинные флаги |
| `semaphore user -add` | `semaphore user add` | Подкоманды вместо флагов |

### API

API полностью совместим с Go-версией. Все endpoints работают аналогично.

### Веб-интерфейс

Веб-интерфейс идентичен Go-версии, так как используется тот же frontend (Vue.js).

## 🔧 Решение проблем

### Ошибка: "Database migration failed"

```bash
# Очистите таблицу миграций и попробуйте снова
# Для SQLite:
sqlite3 /var/lib/semaphore/semaphore.db "DROP TABLE IF EXISTS migration;"

# Для MySQL/PostgreSQL:
# Подключитесь к БД и выполните:
DROP TABLE IF EXISTS migration;
```

### Ошибка: "Port already in use"

```bash
# Проверьте, что старая Go-версия остановлена
sudo systemctl stop semaphore

# Или измените порт
export SEMAPHORE_HTTP_PORT=3001
```

### Ошибка: "Permission denied"

```bash
# Проверьте права на файлы БД
sudo chown -R semaphore:semaphore /var/lib/semaphore
sudo chmod -R 755 /var/lib/semaphore
```

## 📊 Мониторинг производительности

### Сравнение потребления ресурсов

```bash
# Go-версия
ps aux | grep semaphore

# Rust-версия (должна потреблять меньше памяти)
ps aux | grep semaphore
```

### Бенчмарки

```bash
# Тестирование API
ab -n 1000 -c 10 http://localhost:3000/api/health

# Сравните результаты до и после миграции
```

## 🆘 Поддержка

Если вы столкнулись с проблемами при миграции:

1. Проверьте [документацию](README.md)
2. Посмотрите [issues на GitHub](https://github.com/semaphoreui/semaphore/issues)
3. Присоединяйтесь к [Discord-сообществу](https://discord.gg/5R6k7hNGcH)

## 📈 После миграции

После успешной миграции:

1. Удалите старую Go-версию
2. Обновите документацию по развёртыванию
3. Обновите CI/CD пайплайны
4. Протестируйте все интеграции

---

**Статус миграции**: 🚧 Rust-версия в активной разработке. Некоторые функции могут быть недоступны.
