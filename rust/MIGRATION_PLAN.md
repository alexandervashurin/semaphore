# 🦀 План Полной Миграции на Rust

**Дата**: 2026-02-27
**Статус**: 🚧 В ПРОЦЕССЕ

---

## 📋 Приоритеты

### 1️⃣ Критичные Ошибки (Блокируют компиляцию)

#### A. Trait Implementation (174 ошибки E0277)

**Проблема**: `SqlStore` не реализует `ProjectInviteManager`

**Решение**:
1. Создать `src/db/sql/project_invite.rs`
2. Реализовать методы:
   - `get_project_invites()`
   - `create_project_invite()`
   - `get_project_invite()`
   - `get_project_invite_by_token()`
   - `update_project_invite()`
   - `delete_project_invite()`

**Файлы**:
- `src/db/sql/project_invite.rs` (новый)
- `src/db/sql/mod.rs` (добавить модуль)

---

#### B. SQLx Type Bounds (160 ошибок E0599)

**Проблема**: `UserTotp` и `UserEmailOtp` не реализуют `sqlx::FromRow`

**Решение**:
1. Проверить, что `#[derive(FromRow)]` есть в моделях
2. Если поля опциональные, добавить `#[sqlx(default)]`
3. Исправить типы полей для совместимости с SQLx

**Файлы**:
- `src/models/user.rs` (проверить `UserTotp`, `UserEmailOtp`)

---

#### C. Field Access Errors (151 ошибка E0609)

**Проблема**: Нет поля в типе

**Примеры**:
```
error[E0609]: no field `max_parallel_tasks` on type `Project`
```

**Решение**:
1. Добавить недостающие поля в модели
2. Или исправить код, использующий поля

**Файлы**:
- `src/models/project.rs` (добавить `max_parallel_tasks`)
- Другие модели по мере выявления

---

#### D. Type Mismatches (93 ошибки E0308)

**Проблема**: Ожидается `Option<T>`, найдено `T`

**Пример**:
```
error[E0308]: mismatched types
  expected `Option<i32>`, found `i32`
```

**Решение**:
1. Обернуть в `Some()` где нужно
2. Или изменить тип поля

**Файлы**:
- `src/services/restore.rs:417`
- Другие по мере выявления

---

### 2️⃣ Средние Ошибки

#### E. Method Signature Errors (80 ошибок E0560, E0282)

**Проблема**: Неправильная сигнатура метода

**Решение**:
1. Сверить с трейтом
2. Исправить параметры

---

#### F. Type Alias Errors (74 ошибки E0107)

**Проблема**: Неправильное использование типа

**Решение**:
1. Проверить импорты
2. Исправить generic параметры

---

### 3️⃣ Минорные Ошибки

#### G. Trait Method Count (19 ошибок E0034)

**Проблема**: Метод имеет неправильное количество параметров

**Решение**:
1. Сверить с трейтом
2. Исправить сигнатуру

---

#### H. Duplicate Definitions (15 ошибок E0592)

**Проблема**: Дублирующиеся определения

**Решение**:
1. Удалить дубликаты
2. Объединить реализации

---

#### I. Trait Item Errors (11 ошибок E0053)

**Проблема**: Неправильная реализация элемента трейта

**Решение**:
1. Проверить сигнатуры
2. Исправить согласно трейту

---

## 🎯 План Работ

### День 1: Критичные Ошибки

**Утро (4 часа)**:
- [ ] Реализовать `ProjectInviteManager` для `SqlStore`
- [ ] Исправить `UserTotp` и `UserEmailOtp` для SQLx
- [ ] Добавить недостающие поля в модели

**День (4 часа)**:
- [ ] Исправить type mismatches (93 ошибки)
- [ ] Исправить method signatures (80 ошибок)

**Вечер (2 часа)**:
- [ ] Исправить type alias errors (74 ошибки)
- [ ] Проверить прогресс: `cargo check 2>&1 | wc -l`

---

### День 2: Средние Ошибки

**Утро (4 часа)**:
- [ ] Исправить trait method errors (19 ошибок)
- [ ] Исправить duplicate definitions (15 ошибок)

**День (4 часа)**:
- [ ] Исправить trait item errors (11 ошибок)
- [ ] Исправить остальные ошибки

**Вечер (2 часа)**:
- [ ] Достичь <100 ошибок
- [ ] Подготовиться к финальной сборке

---

### День 3: Финальная Сборка

**Утро (2 часа)**:
- [ ] Исправить последние ошибки
- [ ] `cargo check` без ошибок ✅

**День (4 часа)**:
- [ ] `cargo build --release` ✅
- [ ] `cargo test` ✅

**Вечер (2 часа)**:
- [ ] Удалить Go модули
- [ ] Обновить документацию

---

### День 4: Завершение

**Утро (2 часа)**:
- [ ] `cargo audit` ✅
- [ ] Исправить уязвимости (если есть)

**День (2 часа)**:
- [ ] Финальный коммит
- [ ] Пуш в репозиторий
- [ ] Релиз v1.0.0 🎉

---

## 📊 Прогресс

| Категория | Ошибок | Исправлено | Осталось | Прогресс |
|-----------|--------|------------|----------|----------|
| **E0277** (Trait bounds) | 174 | 0 | 174 | 0% |
| **E0599** (Method not found) | 160 | 0 | 160 | 0% |
| **E0609** (No field) | 151 | 0 | 151 | 0% |
| **E0308** (Type mismatch) | 93 | 0 | 93 | 0% |
| **E0560/E0282** (Signatures) | 160 | 0 | 160 | 0% |
| **E0107** (Type alias) | 74 | 0 | 74 | 0% |
| **Остальные** | 157 | 0 | 157 | 0% |
| **ВСЕГО** | **969** | **0** | **969** | **0%** |

---

## 🔧 Команды

### Анализ ошибок
```bash
# Посчитать ошибки по типам
cargo check 2>&1 | grep "error\[E" | cut -d: -f1 | sort | uniq -c | sort -rn

# Просмотреть конкретную ошибку
cargo check 2>&1 | grep -A5 "error\[E0277\]"
```

### Исправление
```bash
# Проверка после изменений
cargo check

# Сборка
cargo build --release

# Тесты
cargo test
```

### Удаление Go
```bash
# Удалить pkg
rm -rf pkg/

# Удалить Go модули
rm -rf api/ db/ db_lib/ services/ cli/

# Удалить go.mod
rm go.mod go.sum
```

### Audit
```bash
cargo audit
cargo audit --ignore RUSTSEC-2025-0057 --ignore RUSTSEC-2024-0384
```

---

**Ответственный**: Alexander Vashurin
**Следующий шаг**: Реализация `ProjectInviteManager` для `SqlStore`
