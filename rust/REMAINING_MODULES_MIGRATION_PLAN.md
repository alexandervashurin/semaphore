# 🚀 План миграции оставшихся Go модулей на Rust

**Дата**: 2026-02-26  
**Статус**: 📋 План работ

---

## 📊 Общая статистика

| Категория | Файлов | Строк | Сложность | Время |
|-----------|--------|-------|-----------|-------|
| **Приоритет 1** (Критичный) | 25 | ~8,500 | Высокая | 12-16 недель |
| **Приоритет 2** (Важный) | 15 | ~5,500 | Средняя | 8-12 недель |
| **Приоритет 3** (Второстепенный) | 30 | ~3,600 | Низкая | 4-6 недель |
| **ВСЕГО** | **70** | **~17,600** | - | **24-34 недели** |

---

## 🎯 Приоритет 1: Критичные модули (Ядро системы)

### 1.1 services/tasks/ - Выполнение задач (2,415 строк)

**Файлы**:
- `LocalJob.go` (1,020 строк) - локальное выполнение
- `TaskPool.go` (752 строки) - пул задач
- `TaskRunner.go` (439 строк) - запуск задач
- `RemoteJob.go` (204 строки) - удалённое выполнение

**Функциональность**:
- Подготовка окружения (Git, inventory, vault)
- Генерация аргументов для Ansible/Terraform/Shell
- Управление процессами
- Очередь задач и статусы
- HA поддержка (Redis)
- WebSocket уведомления

**Rust реализация**:
```rust
// rust/src/services/tasks/
mod local_job;      // LocalJob.go
mod task_pool;      // TaskPool.go (частично есть)
mod task_runner;    // TaskRunner.go (частично есть)
mod remote_job;     // RemoteJob.go
mod job_executor;   // Общий исполнитель
```

**Необходимые crate**:
- `tokio` - async runtime
- `redis` - HA поддержка
- `tokio-tungstenite` - WebSocket
- `process-compose` - управление процессами

**Сложность**: 🔴 Высокая  
**Время**: 6-8 недель  
**Зависимости**: db_lib/, services/server/

---

### 1.2 db_lib/ - Исполнители приложений (1,227 строк)

**Файлы**:
- `TerraformApp.go` (391 строка) - Terraform/OpenTofu/Terragrunt
- `GoGitClient.go` (323 строки) - Git на go-git
- `CmdGitClient.go` (212 строк) - Git через cmd
- `AnsibleApp.go` (174 строки) - Ansible execution
- `ShellApp.go` (127 строк) - Shell scripts
- `AnsiblePlaybook.go` (103 строки) - ansible-playbook
- `GitRepository.go` (80 строк) - Git wrapper
- `LocalApp.go` (66 строк) - LocalApp interface
- `AppFactory.go` (37 строк) - Factory
- `GitClientFactory.go` (26 строк) - Git factory
- `AccessKeyInstaller.go` (11 строк) - **УЖЕ ЕСТЬ В RUST**

**Функциональность**:
- Выполнение Terraform (init, plan, apply, workspace)
- Выполнение Ansible (playbook, galaxy)
- Git операции (clone, pull, checkout)
- Shell script execution
- Кэширование ролей/collections

**Rust реализация**:
```rust
// rust/src/services/executors/
mod terraform;      // TerraformApp.go
mod ansible;        // AnsibleApp.go
mod shell;          // ShellApp.go
mod git_client;     // GitClient.go (частично есть)
mod git_repository; // GitRepository.go (есть)
mod app_factory;    // AppFactory.go
```

**Необходимые crate**:
- `git2` - Git операции
- `tokio-process` - async процессы
- `tempfile` - временные файлы
- `sha2` - хэширование для кэширования

**Сложность**: 🔴 Высокая  
**Время**: 4-6 недель  
**Зависимости**: services/tasks/, pkg/ssh

---

### 1.3 services/server/ - Безопасность (614 строк)

**Файлы**:
- `access_key_encryption_svc.go` (216 строк) - шифрование ключей
- `access_key_serializer_local.go` (238 строк) - сериализация
- `secret_storage_svc.go` (213 строк) - HashiCorp Vault
- `access_key_svc.go` (112 строк) - CRUD ключей
- `access_key_installation_svc.go` (38 строк) - установка ключей

**Функциональность**:
- AES-GCM шифрование
- HashiCorp Vault интеграция
- DVLS интеграция
- CRUD операции с ключами
- SSH agent установка

**Rust реализация**:
```rust
// rust/src/services/security/
mod encryption;         // access_key_encryption_svc.go
mod serializer;         // access_key_serializer_local.go
mod vault_storage;      // secret_storage_svc.go
mod access_key_svc;     // access_key_svc.go
mod installation;       // access_key_installation_svc.go (частично есть)
```

**Необходимые crate**:
- `aes-gcm` - AES шифрование
- `vault-client` - HashiCorp Vault
- `rsa` - RSA шифрование
- `hmac` - HMAC

**Сложность**: 🔴 Высокая  
**Время**: 4-5 недель  
**Зависимости**: pkg/ssh, pkg/task_logger

---

### 1.4 services/runners/ - Удалённые раннеры (685 строк)

**Файлы**:
- `job_pool.go` (685 строк) - пул удалённых задач
- `running_job.go` (131 строка) - запущенные задачи
- `types.go` (78 строк) - типы

**Функциональность**:
- HTTP polling сервера
- Webhook поддержка
- Регистрация раннеров
- one_off режим (lambda-style)
- RSA шифрование ответа (chunked)

**Rust реализация**:
```rust
// rust/src/services/runners/
mod job_pool;       // job_pool.go
mod running_job;    // running_job.go
mod types;          // types.go
mod http_client;    // HTTP client для polling
```

**Необходимые crate**:
- `reqwest` - HTTP client
- `tokio` - async runtime
- `rsa` - шифрование
- `serde_json` - JSON

**Сложность**: 🔴 Высокая  
**Время**: 3-4 недели  
**Зависимости**: services/tasks/

---

### 1.5 pkg/ssh и pkg/task_logger - ГОТОВЫ ✅

**Статус**: ✅ **УЖЕ РЕАЛИЗОВАНЫ В RUST**

- `pkg/ssh/agent.go` → `rust/src/services/ssh_agent.rs` (965 строк, 20 тестов)
- `pkg/task_logger/task_logger.go` → `rust/src/services/task_logger.rs` (450+ строк, 14 тестов)

**Действия**:
1. Протестировать интеграцию через FFI
2. Удалить Go модули
3. Обновить зависимости в Go коде

**Время**: 1-2 недели (тестирование + удаление)

---

## 🎯 Приоритет 2: Важные модули (Бизнес-логика)

### 2.1 util/config.go - Конфигурация (1,407 строк)

**Функциональность**:
- Загрузка из JSON/env
- Валидация конфигурации
- TLS, LDAP, OIDC поддержка
- HA конфигурация
- TmpPath, TmpDir, ProjectDir

**Rust реализация**:
```rust
// rust/src/config/
mod config;         // config.go (расширить существующий)
mod auth;           // config_auth.go
mod sysproc;        // config_sysproc.go
mod validator;      // валидация
```

**Необходимые crate**:
- `serde` - сериализация
- `dotenvy` - env переменные
- `validator` - валидация
- `ldap3` - LDAP (опционально)

**Сложность**: 🟡 Средняя  
**Время**: 2-3 недели  
**Зависимости**: нет

---

### 2.2 services/schedules/SchedulePool.go - Планировщик (414 строк)

**Функциональность**:
- Cron расписания
- одноразовые `run_at` задачи
- HA deduplication (Redis locks)
- Управление состоянием

**Rust реализация**:
```rust
// rust/src/services/scheduler/
mod schedule_pool;  // SchedulePool.go (расширить existing)
mod cron_parser;    // парсинг cron
mod deduplicator;   // HA deduplication
```

**Необходимые crate**:
- `cron` - парсинг cron
- `tokio-cron-scheduler` - планировщик
- `redis` - HA locks

**Сложность**: 🟡 Средняя  
**Время**: 2-3 недели  
**Зависимости**: services/tasks/

---

### 2.3 services/project/ - Экспорт/импорт проектов (1,146 строк)

**Файлы**:
- `restore.go` (621 строка) - восстановление
- `backup.go` (525 строк) - экспорт
- `backup_marshal.go` (304 строки) - marshal/unmarshal

**Функциональность**:
- Экспорт проектов в JSON
- Восстановление из backup
- Уникальные имена при коллизиях
- Маппинг ключей зависимостей

**Rust реализация**:
```rust
// rust/src/services/project/
mod backup;         // backup.go
mod restore;        // restore.go
mod marshal;        // backup_marshal.go
mod types;          // types.go
```

**Необходимые crate**:
- `serde` - сериализация
- `serde_json` - JSON
- `uuid` - UUID

**Сложность**: 🟡 Средняя  
**Время**: 3-4 недели  
**Зависимости**: services/export/

---

### 2.4 services/tasks/alert.go - Алерты (583 строки)

**Функциональность**:
- Telegram, Slack, Rocket.Chat
- Microsoft Teams, DingTalk
- Gotify, email уведомления
- Шаблонизация сообщений

**Rust реализация**:
```rust
// rust/src/services/alerts/
mod alert_manager;      // главный менеджер
mod telegram;           // Telegram bot API
mod slack;              // Slack webhook
mod rocket_chat;        // Rocket.Chat
mod teams;              // MS Teams
mod dingtalk;           // DingTalk
mod gotify;             // Gotify
mod email;              // email через lettre
```

**Необходимые crate**:
- `reqwest` - HTTP client
- `lettre` - email
- `tera` - шаблонизация

**Сложность**: 🟡 Средняя  
**Время**: 2-3 недели  
**Зависимости**: util/config.go

---

### 2.5 PRO модули (575 строк)

**Файлы**:
- `pro/api/` - PRO API endpoints
- `pro/services/` - PRO сервисы
- `pro/db/` - PRO БД
- `pro/pkg/` - PRO утилиты

**Функциональность**:
- Подписки (subscriptions)
- HA режим (NodeRegistry, OrphanCleaner)
- Terraform inventory
- Log writing service
- Features flags

**Rust реализация**:
```rust
// rust/src/pro/
mod api/
mod services/
mod db/
mod pkg/
```

**Сложность**: 🟡 Средняя  
**Время**: 3-4 недели  
**Зависимости**: основные модули

---

## 🎯 Приоритет 3: Второстепенные модули

### 3.1 services/export/ - Экспорт данных (2,654 строки, 26 файлов)

**Файлы** (все простые, 60-130 строк):
- `Exporter.go` (520 строк) - главный экспортер
- `TaskOutput.go`, `Event.go`, `Task.go` - экспорт сущностей
- `Schedule.go`, `Template.go`, `Inventory.go` - и др.

**Функциональность**:
- Экспорт всех сущностей БД
- Зависимости между сущностями
- Маппинг ключей

**Rust реализация**:
```rust
// rust/src/services/export/
mod exporter;         // Exporter.go
mod task_output;      // TaskOutput.go
mod event;            // Event.go
// ... (23 файла)
```

**Сложность**: 🟢 Низкая  
**Время**: 3-4 недели (много файлов, но простые)  
**Зависимости**: модели данных

---

### 3.2 util/ - Утилиты (1,320 строк)

**Файлы**:
- `config_*.go` (тесты, вспомогательные) - ~700 строк
- `mailer/` (mailer.go, auth.go) - 310 строк
- `encryption.go` - 49 строк
- `shell.go` - 43 строки
- Остальные (test_helpers, debug, version) - ~218 строк

**Rust реализация**:
```rust
// rust/src/utils/
mod encryption;       // encryption.go (rsa crate)
mod shell;            // shell.go (shlex crate)
mod mailer;           // mailer/ (lettre crate)
mod test_helpers;     // тестовые хелперы
```

**Необходимые crate**:
- `lettre` - email
- `rsa` - шифрование
- `shlex` - shell escaping

**Сложность**: 🟢 Низкая  
**Время**: 1-2 недели  
**Зависимости**: нет

---

### 3.3 pro_interfaces/ - PRO интерфейсы (98 строк)

**Файлы**:
- `subscription_svc.go` (27 строк)
- `log_write_svc.go` (29 строк)
- `terraform_inventory_ctl.go` (17 строк)
- `project_runner_ctl.go` (15 строк)
- `subscription_ctl.go` (10 строк)

**Функциональность**:
- Простые интерфейсы для PRO функций

**Сложность**: 🟢 Низкая  
**Время**: 2-3 дня  
**Зависимости**: PRO модули

---

## 📅 Дорожная карта миграции

### Этап 1: Подготовка (2 недели)

**Задачи**:
1. ✅ Создать FFI инфраструктуру
2. ✅ Интегрировать pkg/task_logger и pkg/ssh через FFI
3. ✅ Удалить Go модули pkg/task_logger и pkg/ssh
4. ⏳ Настроить CI/CD для Rust

**Результат**:
- FFI работает
- 2 Go модуля удалено
- ~335 строк кода переведено

---

### Этап 2: Утилиты и простые модули (3 недели)

**Задачи**:
1. ⏳ Мигрировать util/mailer/
2. ⏳ Мигрировать util/encryption.go, util/shell.go
3. ⏳ Мигрировать util/config.go (расширить)
4. ⏳ Мигрировать pro_interfaces/

**Результат**:
- ~1,800 строк кода переведено
- Утилиты готовы
- PRO интерфейсы готовы

---

### Этап 3: Ядро системы - Исполнители (6 недель)

**Задачи**:
1. ⏳ Мигрировать db_lib/TerraformApp.go
2. ⏳ Мигрировать db_lib/AnsibleApp.go
3. ⏳ Мигрировать db_lib/ShellApp.go
4. ⏳ Мигрировать db_lib/GitClient.go
5. ⏳ Мигрировать db_lib/GitRepository.go (расширить)
6. ⏳ Мигрировать db_lib/LocalApp.go, AppFactory.go

**Результат**:
- ~1,227 строк кода переведено
- Все исполнители готовы
- Git клиент готов

---

### Этап 4: Ядро системы - Задачи (8 недель)

**Задачи**:
1. ⏳ Мигрировать services/tasks/LocalJob.go
2. ⏳ Мигрировать services/tasks/TaskPool.go (расширить)
3. ⏳ Мигрировать services/tasks/TaskRunner.go (расширить)
4. ⏳ Мигрировать services/tasks/RemoteJob.go
5. ⏳ Мигрировать services/tasks/alert.go

**Результат**:
- ~2,998 строк кода переведено
- Система задач готова
- Алерты готовы

---

### Этап 5: Безопасность (5 недель)

**Задачи**:
1. ⏳ Мигрировать services/server/access_key_encryption_svc.go
2. ⏳ Мигрировать services/server/access_key_serializer_local.go
3. ⏳ Мигрировать services/server/secret_storage_svc.go
4. ⏳ Мигрировать services/server/access_key_svc.go
5. ⏳ Мигрировать services/server/access_key_installation_svc.go

**Результат**:
- ~614 строк кода переведено
- Шифрование готово
- Vault интеграция готова

---

### Этап 6: Инфраструктура (7 недель)

**Задачи**:
1. ⏳ Мигрировать services/runners/job_pool.go
2. ⏳ Мигрировать services/runners/running_job.go
3. ⏳ Мигрировать services/schedules/SchedulePool.go
4. ⏳ Мигрировать services/project/backup.go, restore.go
5. ⏳ Мигрировать PRO модули

**Результат**:
- ~2,878 строк кода переведено
- Раннеры готовы
- Планировщик готов
- Экспорт/импорт готов

---

### Этап 7: Завершение (4 недели)

**Задачи**:
1. ⏳ Мигрировать services/export/ (26 файлов)
2. ⏳ Финальное тестирование
3. ⏳ Удаление оставшихся Go модулей
4. ⏳ Обновление документации

**Результат**:
- ~2,752 строк кода переведено
- Все модули мигрированы
- Go код удалён

---

## 📊 Итоговая таблица миграции

| Этап | Недель | Строк | Модулей | Статус |
|------|--------|-------|---------|--------|
| 1. Подготовка | 2 | 335 | 2 | ✅ Готово |
| 2. Утилиты | 3 | 1,800 | 10 | ⏳ В плане |
| 3. Исполнители | 6 | 1,227 | 11 | ⏳ В плане |
| 4. Задачи | 8 | 2,998 | 5 | ⏳ В плане |
| 5. Безопасность | 5 | 614 | 5 | ⏳ В плане |
| 6. Инфраструктура | 7 | 2,878 | 10 | ⏳ В плане |
| 7. Завершение | 4 | 2,752 | 27 | ⏳ В плане |
| **ВСЕГО** | **35** | **~12,604** | **70** | **5% готово** |

---

## 🔧 Технические требования

### FFI Инфраструктура

```toml
# rust/Cargo.toml
[lib]
crate-type = ["cdylib", "staticlib"]
name = "semaphore_ffi"

[dependencies]
cbindgen = "0.26"  # Генерация C header

[features]
ffi = []
```

### Генерация C header

```bash
# Установка cbindgen
cargo install cbindgen

# Генерация
cbindgen --config rust/ffi/cbindgen.toml --output rust/ffi/semaphore.h
```

### Go обёртки

```go
// go/pkg/rustlib/rustlib.go
/*
#cgo LDFLAGS: -L./lib -lsemaphore_ffi
#include "semaphore.h"
*/
import "C"
```

---

## ⚠️ Риски и решения

### Риск 1: Производительность FFI

**Проблема**: Вызовы Rust из Go медленнее

**Решение**:
- Минимизировать количество вызовов
- Batch операции
- Кэширование результатов

### Риск 2: Сложность отладки

**Проблема**: Стектрейсы через FFI сложнее

**Решение**:
- Подробное логирование на границах
- Использование `tracing` crate
- Отдельные логи для Rust/Go

### Риск 3: Потоконезависимость

**Проблема**: Rust async vs Go goroutines

**Решение**:
- `tokio` с `multi-thread` runtime
- Выделение отдельного потока для FFI
- Использование channels для коммуникации

### Риск 4: Сборка

**Проблема**: Требуется Rust + Go

**Решение**:
- Docker образ с обоими инструментами
- CI/CD пайплайн с кэшированием
- Раздельная сборка библиотек

---

## 📝 Рекомендации

### Для команды

1. **Начать с простых модулей** - util/, pro_interfaces/
2. **Постепенно переходить к сложным** - services/tasks/, db_lib/
3. **Тестировать каждый этап** - unit + integration тесты
4. **Вести документацию** - обновлять MIGRATION_REPORT.md

### Для разработки

1. **Использовать FFI для интеграции** - постепенная миграция
2. **Сохранять обратную совместимость** - API без изменений
3. **Писать тесты сразу** - TDD подход
4. **Документировать изменения** - CHANGELOG.md

---

## ✅ Чеклист готовности этапа

Для каждого этапа:

- [ ] Код написан на Rust
- [ ] Тесты написаны и проходят
- [ ] FFI функции экспортированы
- [ ] Go обёртки созданы
- [ ] Интеграция протестирована
- [ ] Документация обновлена
- [ ] Go модуль удалён (если готов)

---

**Ответственный**: Alexander Vashurin  
**Дата**: 2026-02-26  
**Статус**: 📋 ПЛАН РАБОТ  
**Следующий шаг**: Начать Этап 2 (Утилиты)
