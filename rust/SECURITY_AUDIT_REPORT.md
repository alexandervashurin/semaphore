# 🔒 Отчёт о Безопасности Semaphore UI (Rust)

**Дата**: 2026-02-27
**Инструмент**: `cargo-audit`

---

## 📊 Результаты Аудита

### Уязвимости (1 найдено)

#### 🔴 RUSTSEC-2023-0071: Marvin Attack в `rsa`

**Пакет**: `rsa 0.9.10`
**Серьёзность**: 5.9 (medium)
**Дата**: 2023-11-22
**URL**: https://rustsec.org/advisories/RUSTSEC-2023-0071

**Описание**:
Атака Marvin - это атака по времени на RSA decryption, которая потенциально позволяет восстановить приватный ключ через анализ времени выполнения операций.

**Зависимости**:
```
rsa 0.9.10
├── sqlx-mysql 0.8.6
│   ├── sqlx-macros-core 0.8.6
│   │   └── sqlx-macros 0.8.6
│   │       └── sqlx 0.8.6
│   └── sqlx 0.8.6
└── semaphore 0.1.0 (прямая зависимость)
```

**Решение**:
На данный момент доступного исправления нет! (`No fixed upgrade is available!`)

**Митигация**:
1. **Не использовать RSA decryption** в критичных по времени операциях
2. **Использовать RSA только для шифрования**, не для расшифровки в реальном времени
3. **Мониторить обновления** `rsa` crate
4. Рассмотреть альтернативы:
   - `age` - современный формат шифрования
   - `libsodium` / `sodiumoxide` - криптографическая библиотека

**Влияние на проект**:
- ✅ **Низкое** - RSA используется только для генерации ключей в `utils/encryption.rs`
- ✅ **Не используется для расшифровки** в реальном времени
- ✅ **Ключи генерируются один раз** и хранятся в защищённом хранилище

---

### Предупреждения (2 найдено)

#### 🟡 RUSTSEC-2025-0057: `fxhash` unmaintained

**Пакет**: `fxhash 0.2.1`
**Статус**: Не поддерживается
**Дата**: 2025-09-05

**Зависимости**:
```
fxhash 0.2.1
└── sled 0.34.7
    └── semaphore 0.1.0
```

**Решение**:
- Заменить `sled` на альтернативу (например, `redb` или `rocksdb`)
- Или использовать `fxhash` форк с поддержкой

**Влияние**:
- ⚠️ **Среднее** - `sled` используется для BoltDB-совместимого хранилища
- 📋 **План**: Миграция на `redb` в будущем

---

#### 🟡 RUSTSEC-2024-0384: `instant` unmaintained

**Пакет**: `instant 0.1.13`
**Статус**: Не поддерживается
**Дата**: 2024-09-01

**Зависимости**:
```
instant 0.1.13
├── parking_lot_core 0.8.6
│   └── parking_lot 0.11.2
│       └── sled 0.34.7
│           └── semaphore 0.1.0
└── parking_lot 0.11.2
```

**Решение**:
- Обновить `parking_lot` до версии 0.12+ (использует `std::time::Instant`)
- Или заменить `sled` на альтернативу

**Влияние**:
- ⚠️ **Низкое** - `instant` используется только для таймеров
- 📋 **План**: Обновить зависимости при обновлении `sled`

---

## ✅ Положительные Результаты

### Криптография

#### `encryption.rs` - Анализ Кода

**Файл**: `rust/src/utils/encryption.rs`

**Реализовано**:
- ✅ RSA 2048-bit ключи
- ✅ PEM формат (PKCS#1)
- ✅ Криптографически стойкий генератор (`OsRng`)
- ✅ Правильная обработка ошибок
- ✅ Полное покрытие тестами (3 теста)

**Безопасность**:
- ✅ Использует `OsRng` (криптографически стойкий RNG)
- ✅ Ключи 2048 бит (минимум для RSA)
- ✅ PEM формат для совместимости
- ✅ Нет уязвимостей в коде

**Рекомендации**:
- ✅ **Готово к использованию**
- ⚠️ Рассмотреть переход на RSA 4096 бит для повышенной безопасности
- ⚠️ Добавить проверку стойкости ключей

---

## 📋 Рекомендации

### Краткосрочные (1-2 недели)

1. **Мониторинг `rsa` crate**:
   - Подписаться на обновления
   - Проверять GitHub Issues
   - Следить за RustSec advisory

2. **Обновление зависимостей**:
   ```bash
   cargo update -p parking_lot
   cargo update -p sled
   ```

3. **Документирование**:
   - Добавить SECURITY.md
   - Создать политику обновления зависимостей

### Долгосрочные (1-3 месяца)

1. **Замена `sled`**:
   - Оценить `redb` (pure Rust, активная поддержка)
   - Оценить `rocksdb` (высокая производительность)
   - Создать план миграции

2. **Альтернативы RSA**:
   - Исследовать `age` для шифрования
   - Рассмотреть `libsodium` для общей криптографии
   - Оценить влияние на совместимость

3. **Автоматизация**:
   - Настроить GitHub Actions для `cargo audit`
   - Интегрировать в CI/CD pipeline
   - Настроить уведомления о новых уязвимостях

---

## 🔧 Команды для Проверки

### Запуск аудита
```bash
cd rust
cargo audit
```

### Обновление зависимостей
```bash
# Обновить всё
cargo update

# Обновить конкретный пакет
cargo update -p rsa
cargo update -p sled
cargo update -p parking_lot
```

### Проверка уязвимостей
```bash
# Полная проверка
cargo audit --db ./advisory-db

# Проверка с игнорированием предупреждений
cargo audit --ignore RUSTSEC-2025-0057 --ignore RUSTSEC-2024-0384
```

---

## 📞 Контакты

**Ответственный**: Alexander Vashurin
**Репозиторий**: https://github.com/alexandervashurin/semaphore
**Discord**: https://discord.gg/5R6k7hNGcH

---

**Последнее обновление**: 2026-02-27
**Следующая проверка**: 2026-03-06 (еженедельно)
