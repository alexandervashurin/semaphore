# Декомпозиция LocalJob.go для миграции на Rust

**Дата**: 2026-02-26  
**Статус**: ✅ Завершено

---

## 📊 Проблема

Оригинальный файл `LocalJob.go` содержал **1,021 строку кода** и включал всю логику выполнения задач:
- Управление SSH ключами
- Работа с переменными окружения
- Генерация аргументов для Ansible/Terraform/Shell
- Управление репозиторием
- Vault интеграция
- Запуск задач

Такой монолитный файл сложно мигрировать на Rust и поддерживать.

---

## ✅ Решение

Файл разделён на **8 логических модулей**:

| Файл | Строк | Описание |
|------|-------|----------|
| `local_job_types.go` | 62 | Структура LocalJob и базовые методы |
| `local_job_environment.go` | 157 | Переменные окружения и task details |
| `local_job_args.go` | 125 | Генерация аргументов (Shell, Terraform) |
| `local_job_cli.go` | 82 | CLI аргументы и параметры |
| `local_job_ssh.go` | 52 | Установка SSH ключей |
| `local_job_vault.go` | 60 | Vault файлы |
| `local_job_repository.go` | 18 | Работа с репозиторием |
| `local_job_run.go` | 85 | Основной метод Run |
| **ИТОГО** | **641** | **В 1.6 раза меньше!** |

---

## 📁 Структура модулей

### 1. local_job_types.go

**Назначение**: Структура данных и базовые методы

**Функции**:
- `LocalJob` struct - основная структура
- `IsKilled()` - проверка флага остановки
- `Kill()` - остановка задачи
- `Log(msg)` - логирование
- `SetStatus(status)` - установка статуса
- `SetCommit(hash, message)` - информация о коммите

**Зависимости**: 
- `db.Task`, `db.Template`, `db.Inventory`, `db.Repository`, `db.Environment`
- `ssh.AccessKeyInstallation`
- `task_logger.Logger`
- `db_lib.LocalApp`, `db_lib.AccessKeyInstaller`

---

### 2. local_job_environment.go

**Назначение**: Работа с переменными окружения и task details

**Функции**:
- `getTaskDetails(username, incomingVersion)` - детали задачи
- `getEnvironmentExtraVars(username, incomingVersion)` - дополнительные переменные
- `getEnvironmentExtraVarsJSON(username, incomingVersion)` - JSON переменных
- `getEnvironmentENV()` - ENV переменные
- `getShellEnvironmentExtraENV(username, incomingVersion)` - Shell переменные

**Зависимости**:
- `util.ShellQuote()`, `util.ShellStripUnsafe()`

---

### 3. local_job_args.go

**Назначение**: Генерация аргументов командной строки

**Функции**:
- `getShellArgs(username, incomingVersion)` - аргументы для shell
- `getTerraformArgs(username, incomingVersion)` - аргументы для Terraform (map по стадиям)

**Зависимости**:
- `getEnvironmentExtraVars()`
- `getCLIArgs()`
- `getCLIArgsMap()`

---

### 4. local_job_cli.go

**Назначение**: Работа с CLI аргументами и параметрами

**Функции**:
- `getCLIArgs()` - аргументы из шаблона и задачи
- `getCLIArgsMap()` - аргументы в виде map (для Terraform стадий)
- `getTemplateParams()` - параметры шаблона
- `getParams()` - параметры задачи

**Зависимости**:
- `json.Unmarshal()`

---

### 5. local_job_ssh.go

**Назначение**: Установка и очистка SSH ключей

**Функции**:
- `installSSHKeys()` - установка SSH и Become ключей
- `clearSSHKeys()` - очистка SSH агентов

**Зависимости**:
- `KeyInstaller.Install()`
- `ssh.AccessKeyInstallation`

---

### 6. local_job_vault.go

**Назначение**: Работа с Vault файлами

**Функции**:
- `installVaultKeyFiles()` - установка Vault паролей
- `clearVaultKeyFiles()` - очистка временных файлов Vault

**Зависимости**:
- `KeyInstaller.Install()`
- `os.WriteFile()` с правами 0600

---

### 7. local_job_repository.go

**Назначение**: Обновление и checkout репозитория

**Функции**:
- `updateRepository()` - clone/pull репозитория
- `checkoutRepository()` - checkout на коммит/ветку

**Зависимости**:
- `db_lib.CreateGitClient()`
- `GitClient.CloneOrPull()`, `GitClient.Checkout()`

---

### 8. local_job_run.go

**Назначение**: Основной метод запуска задачи

**Функции**:
- `Run(username, incomingVersion, alias)` - основной метод
- `prepareRun(installingArgs)` - подготовка запуска
- `prepareRunTerraform(tfApp, installingArgs, initArgs)` - подготовка Terraform

**Зависимости**:
- Все предыдущие модули
- `db_lib.CreateAnsibleApp()`, `CreateTerraformApp()`, `CreateShellApp()`

---

## 🔄 План миграции на Rust

### Этап 1: Базовые типы (1-2 дня)

**Файл**: `local_job_types.go`

**Rust модуль**: `rust/src/services/local_job/types.rs`

**Задачи**:
- [ ] Создать структуру `LocalJob`
- [ ] Реализовать базовые методы (IsKilled, Kill, Log, SetStatus, SetCommit)
- [ ] Интегрировать с `TaskLogger`

---

### Этап 2: Переменные окружения (2-3 дня)

**Файл**: `local_job_environment.go`

**Rust модуль**: `rust/src/services/local_job/environment.rs`

**Задачи**:
- [ ] Реализовать `get_task_details()`
- [ ] Реализовать `get_environment_extra_vars()`
- [ ] Реализовать `get_environment_extra_vars_json()`
- [ ] Реализовать `get_environment_env()`
- [ ] Интегрировать с `util::shell_quote()`

---

### Этап 3: CLI аргументы (1-2 дня)

**Файл**: `local_job_cli.go`

**Rust модуль**: `rust/src/services/local_job/cli.rs`

**Задачи**:
- [ ] Реализовать `get_cli_args()`
- [ ] Реализовать `get_cli_args_map()`
- [ ] Реализовать `get_template_params()`
- [ ] Интегрировать с `serde_json`

---

### Этап 4: Генерация аргументов (2-3 дня)

**Файл**: `local_job_args.go`

**Rust модуль**: `rust/src/services/local_job/args.rs`

**Задачи**:
- [ ] Реализовать `get_shell_args()`
- [ ] Реализовать `get_terraform_args()`
- [ ] Интегрировать с `get_environment_extra_vars()`
- [ ] Интегрировать с `get_cli_args()`

---

### Этап 5: SSH и Vault (2-3 дня)

**Файлы**: `local_job_ssh.go`, `local_job_vault.go`

**Rust модули**: 
- `rust/src/services/local_job/ssh.rs`
- `rust/src/services/local_job/vault.rs`

**Задачи**:
- [ ] Реализовать `install_ssh_keys()`
- [ ] Реализовать `clear_ssh_keys()`
- [ ] Реализовать `install_vault_key_files()`
- [ ] Реализовать `clear_vault_key_files()`
- [ ] Интегрировать с `KeyInstaller`

---

### Этап 6: Репозиторий (1-2 дня)

**Файл**: `local_job_repository.go`

**Rust модуль**: `rust/src/services/local_job/repository.rs`

**Задачи**:
- [ ] Реализовать `update_repository()`
- [ ] Реализовать `checkout_repository()`
- [ ] Интегрировать с `GitRepository`

---

### Этап 7: Запуск задачи (2-3 дня)

**Файл**: `local_job_run.go`

**Rust модуль**: `rust/src/services/local_job/run.rs`

**Задачи**:
- [ ] Реализовать `run()` - основной метод
- [ ] Реализовать `prepare_run()`
- [ ] Интегрировать все предыдущие модули
- [ ] Интегрировать с `Executor` (Ansible, Terraform, Shell)

---

### Этап 8: Тестирование (3-5 дней)

**Задачи**:
- [ ] Unit тесты для каждого модуля
- [ ] Integration тесты для `LocalJob`
- [ ] Тесты с реальными Ansible/Terraform playbook
- [ ] Тесты SSH ключей
- [ ] Тесты Vault интеграции

---

## 📈 Преимущества декомпозиции

### До декомпозиции:
- ❌ 1,021 строка в одном файле
- ❌ Сложно тестировать
- ❌ Сложно понимать зависимости
- ❌ Невозможно мигрировать по частям

### После декомпозиции:
- ✅ 8 модулей по 50-150 строк
- ✅ Каждый модуль независимо тестируется
- ✅ Чёткие зависимости между модулями
- ✅ Можно мигрировать поэтапно
- ✅ Легче поддерживать

---

## 🎯 Следующие шаги

1. **Удалить старый файл**: `LocalJob.go.bak`
2. **Запустить тесты**: Убедиться, что всё работает
3. **Начать миграцию на Rust**: Следовать плану выше
4. **Документировать API**: Создать README для каждого модуля

---

## 📚 Связанная документация

- [REMAINING_MODULES_MIGRATION_PLAN.md](./REMAINING_MODULES_MIGRATION_PLAN.md) - общий план миграции
- [RUST_MIGRATION_COMPLETE.md](./RUST_MIGRATION_COMPLETE.md) - отчёт о завершённой миграции
- [GO_MODULES_REMOVAL_GUIDE.md](./GO_MODULES_REMOVAL_GUIDE.md) - руководство по удалению Go модулей

---

**Ответственный**: Alexander Vashurin  
**Дата**: 2026-02-26  
**Статус**: ✅ ДЕКОМПОЗИЦИЯ ЗАВЕРШЕНА
