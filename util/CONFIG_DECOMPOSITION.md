# Декомпозиция util/config.go для миграции на Rust

**Дата**: 2026-02-26  
**Статус**: 📋 План

---

## 📊 Проблема

Оригинальный файл `util/config.go` содержит **1,407 строк кода** и включает:
- Конфигурация приложения
- Загрузка из JSON/env
- Валидация
- TLS/SSL настройки
- LDAP конфигурация
- OIDC конфигурация
- SMTP настройки
- Настройки аутентификации
- TOTP настройки
- HA (High Availability) настройки

**Это самый сложный файл для миграции!**

---

## ✅ Решение

Разделить на **10 логических модулей**:

| Файл | Строк (Go) | Описание |
|------|------------|----------|
| `config_types.go` | ~150 | Основные типы и структура |
| `config_loader.go` | ~200 | Загрузка из JSON/env |
| `config_validator.go` | ~150 | Валидация конфигурации |
| `config_tls.go` | ~100 | TLS/SSL настройки |
| `config_ldap.go` | ~150 | LDAP конфигурация |
| `config_oidc.go` | ~200 | OIDC конфигурация |
| `config_smtp.go` | ~100 | SMTP настройки |
| `config_auth.go` | ~150 | Аутентификация и TOTP |
| `config_ha.go` | ~100 | HA настройки |
| `config_helpers.go` | ~107 | Утилиты и хелперы |
| **ИТОГО** | **~1,407** | **В 10 раз меньше!** |

---

## 📁 Структура модулей

### 1. config_types.go

**Назначение**: Основные типы и структура Config

**Функции**:
- `Config` struct - основная структура конфигурации
- `LdapMappings` struct - маппинги LDAP
- `OidcProvider` struct - OIDC провайдер
- `EmailConfig` struct - Email настройки
- `ConfigType` enum - тип конфигурации

**Зависимости**: 
- Минимальные

---

### 2. config_loader.go

**Назначение**: Загрузка конфигурации

**Функции**:
- `LoadConfig()` - загрузка из файла
- `loadConfigFromEnv()` - загрузка из переменных окружения
- `parseConfigFile()` - парсинг JSON файла
- `mergeConfigs()` - слияние конфигураций

**Зависимости**:
- `os.ReadFile`
- `json.Unmarshal`
- `env` package

---

### 3. config_validator.go

**Назначение**: Валидация конфигурации

**Функции**:
- `Validate()` - основная валидация
- `validatePaths()` - проверка путей
- `validatePorts()` - проверка портов
- `validateSMTP()` - проверка SMTP
- `validateLDAP()` - проверка LDAP
- `validateOIDC()` - проверка OIDC

**Зависимости**:
- `validator` package
- `net/url`

---

### 4. config_tls.go

**Назначение**: TLS/SSL настройки

**Функции**:
- `parseTlsVersion()` - парсинг версии TLS
- `createTlsConfig()` - создание TLS конфигурации
- Настройки сертификатов

**Зависимости**:
- `crypto/tls`
- `crypto/x509`

---

### 5. config_ldap.go

**Назначение**: LDAP конфигурация

**Функции**:
- `LdapConfig` struct - LDAP настройки
- `validateLdapMappings()` - валидация маппингов
- `getLdapConfig()` - получение конфигурации

**Зависимости**:
- `LdapMappings` struct

---

### 6. config_oidc.go

**Назначение**: OIDC конфигурация

**Функции**:
- `OidcProvider` struct - OIDC провайдер
- `OidcEndpoint` struct - endpoint провайдера
- `getOidcProviders()` - получение провайдеров
- Валидация OIDC настроек

**Зависимости**:
- `golang.org/x/oauth2`

---

### 7. config_smtp.go

**Назначение**: SMTP конфигурация

**Функции**:
- `EmailConfig` struct - Email настройки
- `parseSmtpHost()` - парсинг SMTP хоста
- Валидация SMTP настроек

**Зависимости**:
- `net/smtp`

---

### 8. config_auth.go

**Назначение**: Аутентификация и TOTP

**Функции**:
- `AuthConfig` struct - настройки аутентификации
- `TotpConfig` struct - TOTP настройки
- `validateAuthConfig()` - валидация
- `PasswordLoginDisable` настройка

**Зависимости**:
- `TotpConfig` struct

---

### 9. config_ha.go

**Назначение**: HA (High Availability) настройки

**Функции**:
- `HaConfig` struct - HA настройки
- `useRedis()` - проверка использования Redis
- Настройки кластера

**Зависимости**:
- `redis` package

---

### 10. config_helpers.go

**Назначение**: Утилиты и хелперы

**Функции**:
- `Config.CookieSecure` настройка
- `Config.WebHost` настройка
- `Config.TmpPath` получение
- `Config.GetProjectTmpDir()` - получение директории проекта

**Зависимости**:
- Минимальные

---

## 🔄 План миграции на Rust

### Этап 1: Базовые типы (2 дня)

**Файл**: `rust/src/config/types.rs`

**Задачи**:
- [ ] Создать `Config` struct
- [ ] Создать все подструктуры
- [ ] Реализовать `Default` trait
- [ ] Сериализация/десериализация

---

### Этап 2: Loader (2-3 дня)

**Файл**: `rust/src/config/loader.rs`

**Задачи**:
- [ ] Загрузка из JSON
- [ ] Загрузка из env
- [ ] Слияние конфигураций
- [ ] Интеграция с `serde_json`

---

### Этап 3: Validator (2 дня)

**Файл**: `rust/src/config/validator.rs`

**Задачи**:
- [ ] Основная валидация
- [ ] Валидация путей
- [ ] Валидация портов
- [ ] Интеграция с `validator` crate

---

### Этап 4: TLS (1-2 дня)

**Файл**: `rust/src/config/tls.rs`

**Задачи**:
- [ ] Парсинг версий TLS
- [ ] Создание TLS конфигурации
- [ ] Интеграция с `rustls` или `native-tls`

---

### Этап 5: LDAP (2 дня)

**Файл**: `rust/src/config/ldap.rs`

**Задачи**:
- [ ] LDAP структуры
- [ ] Валидация маппингов
- [ ] Интеграция с `ldap3` (опционально)

---

### Этап 6: OIDC (2-3 дня)

**Файл**: `rust/src/config/oidc.rs`

**Задачи**:
- [ ] OIDC структуры
- [ ] Валидация провайдеров
- [ ] Интеграция с `oidc` crate (опционально)

---

### Этап 7: SMTP (1-2 дня)

**Файл**: `rust/src/config/smtp.rs`

**Задачи**:
- [ ] Email структуры
- [ ] Валидация SMTP
- [ ] Интеграция с `lettre`

---

### Этап 8: Auth (2 дня)

**Файл**: `rust/src/config/auth.rs`

**Задачи**:
- [ ] Auth структуры
- [ ] TOTP настройки
- [ ] Валидация

---

### Этап 9: HA (1-2 дня)

**Файл**: `rust/src/config/ha.rs`

**Задачи**:
- [ ] HA структуры
- [ ] Redis настройки
- [ ] Интеграция с `redis` crate

---

### Этап 10: Helpers (1 день)

**Файл**: `rust/src/config/helpers.rs`

**Задачи**:
- [ ] Утилиты
- [ ] Методы для `Config`
- [ ] Геттеры путей

---

### Этап 11: Тестирование (5-7 дней)

**Задачи**:
- [ ] Unit тесты для каждого модуля
- [ ] Integration тесты
- [ ] Тесты загрузки конфигурации
- [ ] Тесты валидации

---

## 📈 Преимущества декомпозиции

### До декомпозиции:
- ❌ 1,407 строк в одном файле
- ❌ Очень сложно тестировать
- ❌ Много зависимостей
- ❌ Невозможно мигрировать по частям

### После декомпозиции:
- ✅ 10 модулей по 100-200 строк
- ✅ Каждый модуль независимо тестируется
- ✅ Чёткие зависимости
- ✅ Можно мигрировать поэтапно

---

## 🔧 Технические Детали

### Config Structure

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Config {
    pub web_host: String,
    pub tcp_address: String,
    pub database: DatabaseConfig,
    pub ldap: Option<LdapConfig>,
    pub oidc: HashMap<String, OidcProvider>,
    pub smtp: Option<SmtpConfig>,
    pub auth: AuthConfig,
    // ...
}
```

### Loader

```rust
impl Config {
    pub fn load_from_file(path: &str) -> Result<Self> {
        let content = fs::read_to_string(path)?;
        let config: Config = serde_json::from_str(&content)?;
        Ok(config)
    }
    
    pub fn load_from_env() -> Result<Self> {
        // Загрузка из переменных окружения
    }
}
```

---

## 🎯 Следующие шаги

1. **Создать Go модули** (2-3 дня)
2. **Начать миграцию на Rust** (15-20 дней)
3. **Интеграция** (2-3 дня)
4. **Тестирование** (5-7 дней)

---

## ⚠️ Риски

1. **Сложность валидации** - много зависимостей между полями
2. **OIDC/LDAP интеграция** - требуются внешние crate
3. **Обратная совместимость** - нужно сохранить формат конфигурации

---

**Ответственный**: Alexander Vashurin  
**Дата**: 2026-02-26  
**Статус**: 📋 ПЛАН  
**Оценка времени**: 25-35 дней
